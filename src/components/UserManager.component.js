import React from 'react';
import ReactDOM from 'react-dom';

class UserManager extends React.component {
  /*
  The idea for this component is to manage the user state for the application
  Like if the User is Admin or Not and show what they are supposed to be able to see.
  */
  constructor(props) {
    super(props);
    this.state = {};
  }
}